export const description =
    "Initial Gitlab configuration"

# Initial Configuration

## First login

Login to Gitlab using these credentials:
- Username: `root`
- Password: Get from `/etc/gitlab/initial_root_password`