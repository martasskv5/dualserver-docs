export const description =
  'Management control tool for the DualServer environment.'

# Mgmtctl

Mgmtctl is the command-line tool for managing the DualServer environment.

## Overview

This page documents the `mgmtctl` CLI — installation, configuration, and common commands.

## Installation

```bash
# Install mgmtctl
curl -fsSL https://example.com/install.sh | sh
```